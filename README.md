# PyTorch_MNIST_NeuralNetwork
A simple neural network built with PyTorch to classify handwritten digits from the MNIST dataset. This project covers data loading and preprocessing, tensor operations, model architecture, training, loss optimization, and model evaluation. A mini-project for practice.
